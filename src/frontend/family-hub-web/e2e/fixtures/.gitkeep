# Fixtures Directory

This directory contains reusable Playwright test fixtures.

## Fixtures to be created (Phase 2):

- `auth.fixture.ts` - OAuth authentication fixture
- `graphql.fixture.ts` - GraphQL interception fixture
- `rabbitmq.fixture.ts` - RabbitMQ event verification fixture

## Usage Pattern

Fixtures provide dependency injection for tests:

```typescript
import { test, expect } from '../fixtures/auth.fixture';

test('my test', async ({ authenticatedPage, interceptGraphQL }) => {
  // authenticatedPage has OAuth tokens pre-configured
  // interceptGraphQL allows mocking GraphQL responses
});
```
