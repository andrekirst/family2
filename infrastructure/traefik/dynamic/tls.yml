# Dynamic TLS configuration — default certificate for *.dev.andrekirst.de
tls:
  stores:
    default:
      defaultCertificate:
        certFile: /certs/local.pem
        keyFile: /certs/local-key.pem
