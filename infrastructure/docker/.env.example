# Family Hub - Environment Variables
# Docker Compose Configuration
# Copy this file to .env and configure with your values

# PostgreSQL
POSTGRES_PASSWORD=Dev123!

# RabbitMQ
RABBITMQ_PASSWORD=Dev123!

# Zitadel Database
ZITADEL_DB_PASSWORD=Dev123!

# Zitadel Master Key (must be 32 characters)
ZITADEL_MASTERKEY=MasterKey123!MustBe32Characters!

# Zitadel Admin User (initial setup)
ZITADEL_ADMIN_PASSWORD=Admin123!

# ===== SMTP Configuration =====
# Choose one of the options below and uncomment it

# Option 1: MailHog (Fake SMTP for Development - Default)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM_ADDRESS=noreply@familyhub.local
SMTP_FROM_NAME=Family Hub
SMTP_TLS=false
SMTP_USER=
SMTP_PASSWORD=

# Option 2: Gmail SMTP (Uncomment and fill in your credentials)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_FROM_ADDRESS=your-email@gmail.com
# SMTP_FROM_NAME=Family Hub
# SMTP_TLS=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Option 3: Outlook/Office365 SMTP (Uncomment and fill in your credentials)
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_FROM_ADDRESS=your-email@outlook.com
# SMTP_FROM_NAME=Family Hub
# SMTP_TLS=true
# SMTP_USER=your-email@outlook.com
# SMTP_PASSWORD=your-password

# Option 4: IONOS SMTP (Uncomment and fill in your credentials)
# SMTP_HOST=smtp.ionos.de
# SMTP_PORT=465
# SMTP_FROM_ADDRESS=your-email@yourdomain.com
# SMTP_FROM_NAME=Family Hub
# SMTP_TLS=true
# SMTP_USER=your-email@yourdomain.com
# SMTP_PASSWORD=your-password

# Option 5: Custom SMTP Server (Uncomment and configure)
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_FROM_ADDRESS=noreply@yourdomain.com
# SMTP_FROM_NAME=Family Hub
# SMTP_TLS=true
# SMTP_USER=your-smtp-username
# SMTP_PASSWORD=your-smtp-password

# SMTP Reply-To Address (optional)
SMTP_REPLY_TO_ADDRESS=support@familyhub.local
